{% set name = "sci-fab" %}
{% set version = "2.2.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name | replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 0d792b8352479411501c2fc6a2b1c3dfcac6b5ee40cfd43c7de32a74eb01c557

build:
  number: 0
  noarch: python
  entry_points:
    - fab=fab.cui.__main__:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools
    - setuptools-scm
  run:
    - fparser >=0.2
    - python >={{ python_min }},<4.0

test:
  imports:
    - fab
    - fab.parse
  commands:
    - pip check
    - fab --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/Metomi/fab
  summary: Build system for scientific software
  dev_url: https://github.com/metomi/fab/
  license: BSD-3-Clause
  license_file: LICENCE

extra:
  recipe-maintainers:
    - yaswant
    - t00sa
    - MatthewHambley
